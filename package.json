{
  "name": "semantic-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first constructive prompt gateway and MCP policy surface.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "mcp": "node dist/mcp.js",
    "test": "node --import tsx --test test/transform.test.ts test/server.test.ts test/mcp.test.ts",
    "test:coverage": "c8 --all --extension .ts --reporter=text --reporter=lcov node --import tsx --test test/transform.test.ts test/server.test.ts test/mcp.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "c8": "^10.1.3",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
